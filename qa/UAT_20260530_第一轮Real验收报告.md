# UAT 报告：2026-05-30 第一轮 Real 验收

## 1. 测试范围

本轮 UAT 在用户确认真实 API Key 已替换后启动，覆盖以下范围：

| 范围 | 说明 |
|---|---|
| 自动化回归 | 既有 pytest 用例 |
| Real MCP 健康检查 | LLM、N2SQL、SQL、知识库、Web、Time、Text MCP |
| Real workflow | 数据查询、固定日期同环比、闲聊 |
| Streamlit 烟测 | real 模式启动与 HTTP 探针 |

敏感信息策略：不记录真实 API Key、数据库密码、SSH 密码、真实内网主机等敏感值。

## 2. 环境与命令

| 项目 | 值 |
|---|---|
| 测试时间 | 2026-05-30 15:55 CST |
| 工作目录 | `/Users/leo/work/ai_model/Cot_research` |
| 运行模式 | `APP_MODE=real`、`MOCK_ENABLED=false` |
| Dify 开关 | UAT 命令中显式设置 `DIFY_ENABLED=true` |

## 3. 测试结果

| 编号 | 场景 | 结果 | 说明 |
|---|---|---|---|
| UAT-01 | 自动化回归 | PASS | `21 passed, 1 warning in 0.12s` |
| UAT-02 | Real MCP 健康检查 | PASS_WITH_DIFY_FALLBACK | MCP 总体 PASS；Dify 远程检索失败后 fallback 到本地知识 |
| UAT-03 | 真实数据查询 | PASS | 路由 `data_query`，2023 年 6 月中东公司批发量 `2160`、终端量 `2311` |
| UAT-04 | 固定日期同环比 | PASS_WITH_DATA_ISSUE | 路由 `yoy_yoy_analysis`，本期终端量 `2311`，去年同期 `0`，同比百分比无意义 |
| UAT-05 | 闲聊/知识问答 | PASS | 路由 `chat`，模型回答正常 |
| UAT-06 | Streamlit 启动烟测 | TECH_PASS | real 模式启动成功，HTTP 探针返回 `200 text/html` |

## 4. 关键命令与证据

自动化回归：

```bash
python3 -m pytest -q
```

结果：

```text
21 passed, 1 warning in 0.12s
```

Real MCP 健康检查：

```bash
APP_MODE=real MOCK_ENABLED=false DIFY_ENABLED=true python3 scripts/check_mcps.py --mode real
```

结果：

```text
llm                 OK
n2sql               OK
sql_executor        OK
knowledge_retrieval OK
web_search          OK
time                OK
text_analysis       OK

Result: PASS
```

同时观察到：

```text
Dify API retrieve failed ... Falling back to local memory.
```

Streamlit 烟测：

```bash
APP_MODE=real MOCK_ENABLED=false DIFY_ENABLED=true python3 -m streamlit run streamlit_langgraph.py --server.headless true --server.port 8501 --browser.gatherUsageStats false
```

HTTP 探针：

```text
HTTP 200
content_type text/html
```

## 5. OPEN 问题

| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| UAT-OPEN-01 | Dify 远程 host 疑似仍为占位或网络不可达 | 远程知识库未真实命中，当前 `knowledge_retrieval` 走本地 fallback | 替换真实 Dify host 或确认网络路由后复测 |
| UAT-OPEN-02 | 去年同期终端量为 0 | 同比百分比无法计算 | 业务确认显示为 `N/A`、`无意义`，还是采用特定业务口径 |
| UAT-OPEN-03 | Streamlit 仅完成技术烟测 | 还未完成用户侧人工交互和视觉验收 | 用户在浏览器输入 UAT 用例，确认页面体验后再进入 UI 重构 |

## 6. 结论

第一轮 Real UAT 技术主链路可继续推进：模型、数据库、Web MCP、工作流与页面启动均可用。

进入下一步前建议先处理 Dify 远程 host 问题，并确认“去年同期为 0”的同环比业务口径。
