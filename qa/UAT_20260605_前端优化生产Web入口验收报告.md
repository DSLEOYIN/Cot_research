# UAT 报告：2026-06-05 前端优化生产 Web 入口

## 1. 测试范围

本轮 UAT 按 `doc/codex_前端优化版本_20260605.md` 的首批验收问题执行，覆盖：

| 范围 | 说明 |
|---|---|
| 会话管理 | 新建会话、消息与步骤保存、刷新后可从 SQLite 读取 |
| 聊天 API | `POST /api/chat` 调用现有 Skill + MCP 后端 |
| 联网开关 | 开启/关闭对 Skill 路由的影响 |
| 多轮追问 | 复用上文内部数据，开启联网后进入外部对比分析 |
| 前端工程 | 独立 Web 前端工程和核心组件是否齐备 |

敏感信息策略：本轮使用 mock 模式，不涉及真实 API Key、数据库密码、SSH 密码或真实内网主机。

## 2. 环境与命令

| 项目 | 值 |
|---|---|
| 测试时间 | 2026-06-05 15:00 CST |
| 工作目录 | `/Users/leo/work/ai_model/Cot_research` |
| 运行模式 | `APP_MODE=mock`、`MOCK_ENABLED=true` |
| API 测试方式 | FastAPI `TestClient` |
| 前端工具链 | Node v24.14.0；未发现 npm/pnpm/yarn/tsc |

## 3. UAT 用例结果

| 编号 | 场景 | 问题 | 联网开关 | 结果 | 说明 |
|---|---|---|---|---|---|
| UAT-WEB-01 | 内部数据 | `2024年中东公司终端量是多少？` | 关闭 | PASS | 路由 `data_query`，只走内部数据链路 |
| UAT-WEB-02 | 内部 + 外部 | `2024年中东公司终端量是多少？` | 开启 | PASS | 路由 `data_web_compare_analysis`，包含 SQL 与 web_search 步骤 |
| UAT-WEB-03 | 联网公开信息 | `网上搜一下最近中东汽车市场趋势` | 开启 | PASS | 路由 `web_search_answer` |
| UAT-WEB-04 | 联网关闭提示 | `网上搜一下最近中东汽车市场趋势` | 关闭 | PASS | 不选择联网 Skill，返回开启联网搜索提示 |
| UAT-WEB-05 | 多轮追问 | `和竞品对比呢？` | 开启 | PASS | 路由 `web_compare_analysis`，复用上文上下文 |
| UAT-WEB-06 | 同环比 | `2024年中东公司终端量同比去年怎么样？` | 关闭 | PASS | 路由 `yoy_yoy_analysis` |

## 4. 关键证据摘要

自动化回归：

```text
45 passed, 1 warning in 0.24s
```

API 级 UAT 摘要：

```text
内部数据        PASS skill=data_query steps=8
内部+外部       PASS skill=data_web_compare_analysis steps=9
联网公开信息    PASS skill=web_search_answer steps=4
联网关闭提示    PASS skill=None steps=1
同环比          PASS skill=yoy_yoy_analysis steps=6
多轮追问        PASS skill=web_compare_analysis steps=5
```

前端静态检查：

```text
frontend_static= PASS
```

前端工具链探测：

```text
node v24.14.0
npm/pnpm/yarn/tsc not found in PATH
```

## 5. OPEN 问题

| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| UAT-WEB-OPEN-01 | 本机缺少 npm 工具链 | 无法启动 Vite dev server、无法执行前端 build | 安装 npm 或启用可用包管理器后复测 |
| UAT-WEB-OPEN-02 | 视觉 95% 复原未完成截图验收 | 无法确认最终浏览器像素级观感 | 前端可启动后，用 Playwright 对比 `前端原型/web端AI助手_2/index.html` 与新 Web 页面 |
| UAT-WEB-OPEN-03 | 当前 UAT 为 mock 模式 | 尚未验证 real 数据、真实联网与真实数据库 | 真实凭据和网络配置确认后补 Real UAT |
| UAT-WEB-OPEN-04 | SSE 暂为顺序输出 | 前端能显示步骤，但不是 MCP 执行中即时推送 | 下一轮接 `run_agent` callback 实现实时 SSE |

## 6. UAT 结论

本轮 UAT 在 mock 模式下通过 API 级业务验收。会话持久化、联网开关路由、内部数据与外部资料组合分析、多轮追问和同环比路由均符合开发文档约束。

生产 Web 前端已具备工程结构和核心页面组件，但还不能声明视觉验收完成。下一步应补齐 npm 工具链，启动前端页面并完成浏览器截图验收和 Real 模式联调。

## 7. 2026-06-05 复测更新

已由 Codex 在项目内安装临时 npm runtime，并完成前端构建与浏览器级 UAT smoke。

| 复测项 | 结果 | 说明 |
|---|---|---|
| 前端构建 | PASS | `npm run build` 成功，Vite 构建完成 |
| 前端启动 | PASS | Vite dev server 启动于 `http://127.0.0.1:5173/` |
| 后端启动 | PASS | FastAPI mock 服务启动于 `http://127.0.0.1:8010/` |
| 页面打开 | PASS | Playwright 打开页面，标题为 `广汽国际 AI 助手` |
| 交互流程 | PASS | 页面自动创建会话，输入并发送 `2024年中东公司终端量是多少？` |
| 回答渲染 | PASS | 返回 `data_query`，页面展示 8 个思考步骤、业务分析和表格 |
| 截图 | PASS | `output/playwright/chatbi-web-smoke-20260605.png` |

更新后 UAT 结论：mock 模式下 API 级与浏览器级基础验收均通过。仍需补充 Real 模式 UAT，以及基于原型页面的视觉对比截图验收。

## 8. 2026-06-06 结构化图表与响应式复测

本轮补齐结构化 ECharts 渲染，并对生产 Web 页面执行桌面和窄屏浏览器复测。

| 复测项 | 结果 | 说明 |
|---|---|---|
| 自动化回归 | PASS | `55 passed, 1 warning in 0.42s` |
| 前端生产构建 | PASS | TypeScript 与 Vite build 均通过 |
| 数值表格渲染 | PASS | 内部数据回答展示 Markdown 数据表格 |
| ECharts 渲染 | PASS | 表格下方生成 `1` 个 ECharts canvas |
| 桌面响应式 | PASS | 图表尺寸 `778 × 280`，页面无横向溢出 |
| 820px 窄屏响应式 | PASS | 历史侧栏隐藏，图表宽度 `668px`，页面无横向溢出 |

更新后结论：Mock 模式下的结果表格、结构化图表、实时步骤和响应式页面均完成浏览器级验收。Real 模式 UAT 仍依赖真实凭据、业务数据库和联网环境。
