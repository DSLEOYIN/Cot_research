# 后续开发计划_codex_20260522

> 用途：这是后续开发的日常排期和跟踪文档。每天打开后，先看“当前状态总览”和“今日任务”，再更新“每日记录”“问题与阻塞”“待测试清单”。

## 0. 状态说明

任务状态统一使用以下标记：

| 状态 | 含义 |
|---|---|
| 未开始 | 尚未动手 |
| 进行中 | 正在开发 |
| 待测试 | 开发完成，需要人工或自动测试 |
| 有问题 | 已发现问题，需要修复 |
| 已完成 | 开发、测试、验收均完成 |
| 暂缓 | 暂时不做或等待前置条件 |

优先级说明：

| 优先级 | 含义 |
|---|---|
| P0 | 必须先做，影响安全、启动或主链路可用性 |
| P1 | 核心能力增强，影响真实可用性 |
| P2 | 维护性、配置化、治理能力 |
| P3 | 工作流高级能力 |
| P4 | 产品化和生态化增强 |

## 1. 当前状态总览

最后更新时间：2026-06-02

| 模块 | 当前状态 | 负责人 | 备注 |
|---|---|---|---|
| 项目架构阅读 | 已完成 | Codex | 已完成初步阅读和架构评估 |
| 安全配置 | 已完成 | Codex | 已移除源码默认 Dify 凭据与历史文档中的已知敏感值；仍建议轮换曾入库凭据 |
| 依赖补齐 | 已完成 | Codex | 已补 `langgraph`、`pytz`、`python-dotenv`、`typing-extensions`、`sshtunnel` |
| Python 3.9 兼容 | 已完成 | Codex | 当前本机 Python 3.9.6 下测试通过 |
| 完整 mock 模式 | 已完成 | Codex | 已支持 `APP_MODE=mock` 下 LLM/N2SQL/SQL 离线返回 |
| MCP 返回结构标准化 | 已完成 | Codex | `execute_mcp` 已补齐 `success/error/error_type` |
| workflow 错误中断 | 已完成 | Codex | MCP 返回 `success=false` 时中断 workflow，已有自动化测试覆盖 |
| SQL 校验与纠错闭环 | 已完成第一版 | Codex | 已完成只读、多语句、表白名单、行数上限、SQL 执行失败后 1 次 LLM 纠错重试 |
| 配置集中化 | 已完成 | Codex | 新增 `app_config.py` 统一读取模型、数据库、mock、Dify 配置 |
| 联网检索 MCP | 已接入，待 Skill 编排 | Codex | 已接入 Tavily `web_search` MCP，mock/real 健康检查均通过；当前真实对话链路尚未主动调用，疑似 Skill 流程未覆盖联网检索场景 |
| 知识库治理 | 有问题 | Codex | N2SQL 已固定使用 active theme DDL，并将检索结果作为补充上下文；Dify key/dataset 可读取，但远程 host 仍疑似占位，UAT 中 fallback 到本地知识 |
| UAT 验收 | 进行中 | Codex/用户 | 第一轮技术 UAT 已跑通自动化、real MCP、真实 workflow 与 Streamlit 烟测；Dify 远程命中和人工 UI 验收仍需继续 |
| Streamlit UI 重构 | 进行中 | Codex/用户 | 已完成第一轮侧边栏操作区与思考链 UI 修复；仍需继续统一整体视觉、交互反馈和结果展示 |

## 1.1 2026-05-30 最新核验

本次处理结果：

| 项目 | 结果 |
|---|---|
| Python 3.9 兼容 | 已修复运行时代码的延迟注解与 `NotRequired` fallback |
| 安全脱敏 | 已移除已知 Dify token、SSH 密码、内网主机硬编码默认值 |
| Dify 默认策略 | 默认关闭，必须显式提供 `DIFY_ENABLED=true`、`DIFY_BASE_URL`、`DIFY_API_KEY`、`DIFY_DATASET_ID` |
| N2SQL 上下文 | Skill 不再把检索结果当表结构覆盖 DDL，而是作为 `context` 传入 |
| LLM 模板变量 | `llm` MCP 新增 `template_vars`，数据解读/口径说明能拿到结构化 SQL 与结果 |
| Tavily 联网 MCP | `web_search` MCP 已接入 Tavily，`WEB_MCP_ENABLED=true` 后 real 测试通过 |
| 自动化测试 | `python3 -m pytest -q`：21 passed，1 warning |
| MCP 健康检查 | `python3 scripts/check_mcps.py`：mock 与 real-missing-config 均 PASS；`python3 scripts/check_mcps.py --mode real` PASS |
| SIT 文档 | 已新增 `qa/SIT_20260530_全量MCP与Real工作流测试报告.md` |
| 工作日志 | 已新增 `history/8_20260530_全量MCP与Real工作流测试日志.md` |

### 1.2 2026-05-30 UAT 启动状态

用户已完成真实 API Key 替换。当前 UAT 策略是不在日志或报告中记录任何真实密钥、密码、主机细节，只记录脱敏后的连通性与业务结果。

UAT 启动前置状态：

| 前置项 | 状态 | 备注 |
|---|---|---|
| 模型 API Key | 已通过 UAT-02/UAT-03/UAT-04/UAT-05 核验 | 仅确认运行环境可读取，不记录真实值 |
| 数据库连接配置 | 已通过 UAT-02/UAT-03/UAT-04 核验 | 包含 DB 与 SSH 隧道配置，仅做脱敏连通性确认 |
| Tavily/Web MCP Key | 已通过 UAT-02 核验 | `WEB_MCP_ENABLED` 当前可读取为开启 |
| Dify URL/API Key/Dataset | 有问题 | key/dataset 可读取，但 real UAT 连接到占位 host 后 fallback；需替换真实 Dify host 或确认网络路由 |
| UAT 测试报告 | 已新增 | `qa/UAT_20260530_第一轮Real验收报告.md` |

UAT 第一轮用例：

| 编号 | 场景 | 输入/命令 | 状态 | 验收标准 |
|---|---|---|---|---|
| UAT-01 | 自动化回归 | `python3 -m pytest -q` | PASS | 21 passed，1 warning |
| UAT-02 | real MCP 健康检查 | `APP_MODE=real MOCK_ENABLED=false DIFY_ENABLED=true python3 scripts/check_mcps.py --mode real` | PASS_WITH_DIFY_FALLBACK | LLM、N2SQL、SQL、Web、Time、Text 均 OK；知识库 MCP OK 但 Dify 远程未真实命中 |
| UAT-03 | 真实数据查询 | `2023年6月中东公司的销量（包括批发量和终端量）是多少？` | PASS | 路由 `data_query`，返回批发量 2160、终端量 2311 |
| UAT-04 | 固定日期同环比 | `2023年6月中东公司终端量同比去年怎么样？` | PASS_WITH_DATA_ISSUE | 路由 `yoy_yoy_analysis`；本期终端量 2311，去年同期 0，同比百分比无意义 |
| UAT-05 | 闲聊/知识问答 | `汽车保养一般多少公里做一次？` | PASS | 路由 `chat`，模型回答正常 |
| UAT-06 | Streamlit 页面验收 | real 模式启动并访问页面 | TECH_PASS | HTTP 200；仍需用户人工输入与视觉验收 |

下一步优先级：

| 顺序 | 任务 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| 1 | Streamlit UI 交互验收与第二轮修复 | P1 | 进行中 | 在内置浏览器中逐项检查侧边栏、输入框、思考链、答案卡片、错误态，发现问题即修 |
| 2 | 前端整体样式重构 | P1 | 进行中 | 重构 Streamlit 页面视觉、布局、侧边栏、思考链展示和结果呈现，形成统一组件风格 |
| 3 | Dify 远程知识库真实验证 | P1 | 有问题 | key/dataset 可读取，但远程 host 疑似占位；替换真实 host 后复测 |
| 4 | 轮换曾写入仓库的 Dify/SSH 凭据 | P0 | 等待用户/运维 | 旧凭据失效，新凭据只在 `.env` 或密钥管理中保存 |
| 5 | 同环比业务口径确认 | P1 | 待用户/业务确认 | 去年同期为 0 时，同比百分比应显示为无意义、N/A，还是按业务规则另行描述 |
| 6 | 清理过期历史文档状态 | P2 | 未开始 | 历史报告保留结论但不包含真实主机、token、密码 |

### 1.3 2026-06-01 前端重构推进状态

本次处理结果：

| 项目 | 结果 |
|---|---|
| 代码同步 | 已推送到 `origin/main`，最新提交 `962c712 Improve Streamlit chat UI traces` |
| 侧边栏会话操作区 | 已移除不稳定 emoji 按钮，改为等高纯文本按钮：置顶、改名、删除 |
| 思考链完成态 UI | 已移除 Streamlit 默认 `st.expander` 展示方式，改为 Codex 风格 timeline |
| 思考链运行态 UI | 已移除 `st.status` 旧展示方式，改为同一套 timeline 容器，并在 callback 中逐步刷新 |
| 聊天主区域视觉 | 已完成第二轮收敛：统一结果分区标题、口径说明卡片、上下文条和首页空态 |
| 推荐问题交互 | 已修复点击推荐问题后首页空态残留的问题，改为 pending query 后重新渲染聊天态 |
| 侧边栏信息架构 | 已完成第一轮降噪：标题、会话、业务场景、运行配置、高级配置分层更清晰 |
| 本地启动策略 | Streamlit 通过 `--server.address 127.0.0.1 --browser.gatherUsageStats false` 启动更稳定，避免外部网络探测拖慢 |
| 静态验证 | `python3 -m py_compile streamlit_langgraph.py streamlit_ui_helpers.py` 通过；`git diff --check` 通过 |
| 自动化测试 | `python3 -m pytest -q`：24 passed，1 warning |
| 浏览器验收 | mock 模式推荐问题、思考链折叠/展开、窄屏无横向溢出已通过技术验收 |

当前前端待继续确认：

| 顺序 | 检查项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 运行中思考链是否实时可见 | 技术通过，待用户确认 | mock 数据查询中 timeline 可见；真实慢查询仍可继续人工观察逐步追加 |
| 2 | 思考链折叠/展开交互 | 技术通过 | 原生 `details/summary` 点击可展开和收起 |
| 3 | 聊天气泡与答案卡片视觉 | 技术通过，待用户确认 | 已统一结果分区标题、图表标题、业务解读和口径说明层级 |
| 4 | 输入框与底部固定区域 | 技术通过，待用户确认 | 510px 窄屏下无横向溢出，底部输入区未遮挡当前内容 |
| 5 | 侧边栏整体信息架构 | 已完成第一轮，待用户确认 | 已把业务主题、运行配置、高级配置做分层降噪 |

## 2. 本周目标

周期：2026-06-01 至 2026-06-07

本周目标从“主链路可运行”转为“前端可用、可验收、可持续迭代”。先把 Streamlit 页面从演示样式整理成稳定的 ChatBI 工作台，再继续处理 Dify 真实命中和业务口径问题。

本周必须完成：

| 编号 | 目标 | 状态 | 验收标准 |
|---|---|---|---|
| W1 | 完成思考链运行态/完成态验收 | 进行中 | 执行过程中 timeline 可见、可展开/折叠，完成后历史消息保持一致样式 |
| W2 | 重构聊天主区域视觉 | 技术通过，待用户确认 | 用户气泡、助手答案、数据表、图表、业务解读层级清晰 |
| W3 | 重构输入框和底部固定区 | 技术通过，待用户确认 | 窄屏不遮挡内容，发送按钮、加载态、禁用态清楚 |
| W4 | 侧边栏信息架构降噪 | 已完成第一轮，待用户确认 | 场景主题、凭证配置、高级配置层级清楚，常用操作更轻 |
| W5 | Dify 远程知识库真实验证 | 有问题 | 替换真实 Dify host 或确认网络路由后，`knowledge_retrieval` 真实命中 |
| W6 | README/运行说明更新 | 未开始 | 新人能按说明启动 mock/real，并知道推荐的本地 Streamlit 启动参数 |
| W7 | 联网检索 Skill 编排补齐 | 未开始 | `web_search` MCP 不只通过健康检查，还能在需要实时信息的问题中被 Skill 主动调用 |

## 3. 今日任务

日期：2026-06-01

| 顺序 | 任务 | 优先级 | 状态 | 产出文件 | 需要测试 |
|---|---|---|---|---|---|
| 1 | 复验思考链运行态实时展示 | P1 | 技术通过，待用户确认 | `streamlit_langgraph.py` | 用数据查询或 real 模式问题观察 callback 是否逐步刷新 |
| 2 | 重构聊天主区域视觉 | P1 | 技术通过，待用户确认 | `streamlit_langgraph.py`、`streamlit_ui_helpers.py` | 浏览器检查用户气泡、助手答案、数据表和业务解读 |
| 3 | 重构输入框和底部固定区 | P1 | 技术通过，待用户确认 | `streamlit_langgraph.py` | 窄屏下输入框不遮挡内容，发送按钮状态清晰 |
| 4 | 侧边栏信息架构降噪 | P1 | 已完成第一轮，待用户确认 | `streamlit_langgraph.py` | 常用区和开发者配置分层，默认状态更简洁 |
| 5 | 更新过程文档和下一步排期 | P1 | 进行中 | `doc/后续开发计划_codex_20260522.md` | 用户确认下一步计划是否符合预期 |

今日完成记录：

| 时间 | 完成内容 | 备注 |
|---|---|---|
| 2026-05-22 | 完成项目全面阅读和架构评估 | 已识别安全、执行器、N2SQL、配置、mock、UI 等问题 |
| 2026-05-22 | 创建后续开发计划文档 | 第一版为规划型文档 |
| 2026-05-22 | 将文档改为日常排期跟踪版 | 增加状态、每日任务、问题、待测试清单 |
| 2026-05-22 | 完成安全配置、依赖、mock 和 MCP 失败中断第一轮实现 | 已通过 `APP_MODE=mock` 的 `run_agent` 基础验证 |
| 2026-05-22 | 新增 MCP 健康检查脚本 | `python scripts/check_mcps.py` 已通过 mock 全 MCP 与 real 缺配置检查 |
| 2026-05-22 | 新增 pytest 自动化基线 | `pytest -q` 当前 14 个用例通过 |
| 2026-05-22 | 增加 SQL 安全第一版 | 已覆盖 SELECT-only、多语句拦截、表白名单、结果行数上限 |
| 2026-06-01 | 完成 Streamlit UI 第一轮修复并推送 | 提交 `962c712`，包含侧边栏按钮和思考链运行态/完成态重构 |
| 2026-06-01 | 更新下一步开发计划 | 将本周目标和今日任务切换到前端重构与 UAT 验收 |
| 2026-06-01 | 完成 Streamlit UI 第二轮技术验收 | 新增 `streamlit_ui_helpers.py` 与测试，收敛聊天主区、输入区、侧边栏和推荐问题交互 |

## 4. 里程碑排期

### M1：安全与可运行基线

目标日期：2026-05-22 至 2026-05-24

| 编号 | 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| M1-1 | 新增 `.env.example` | P0 | 待测试 | 无 | 包含 DB、DeepSeek、mock 相关配置示例 |
| M1-2 | 移除硬编码 DB 凭据 | P0 | 待测试 | M1-1 | 源码无真实 DB 密码 |
| M1-3 | 新增统一配置读取模块 | P0 | 待测试 | M1-1 | 数据库、模型、mock 开关从统一入口读取 |
| M1-4 | 补齐依赖 | P0 | 待测试 | 无 | `langgraph`、`pytz`、可选 `python-dotenv` 已声明 |
| M1-5 | 确认本地启动命令 | P0 | 未开始 | M1-4 | `streamlit run streamlit_langgraph.py` 可启动 |

需要用户测试：

| 测试项 | 状态 | 操作 | 期望结果 |
|---|---|---|---|
| 启动 Streamlit | 未开始 | 运行 `streamlit run streamlit_langgraph.py` | 页面正常打开 |
| 无 `.env` 启动 | 未开始 | 不配置真实凭据直接启动 | 应提示缺少配置或进入 mock，不报异常 |
| 检查敏感信息 | 未开始 | 搜索源码中的 DB 密码 | 不应搜到真实密码 |

### M2：完整 mock 演示链路

目标日期：2026-05-24 至 2026-05-25

| 编号 | 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| M2-1 | 增加 mock 模式开关 | P0 | 待测试 | M1-3 | 支持 `APP_MODE=mock` 或类似配置 |
| M2-2 | mock LLM 返回 | P0 | 待测试 | M2-1 | `llm` 在 mock 模式不请求外部 API |
| M2-3 | mock N2SQL 返回 | P0 | 待测试 | M2-1 | 数据查询问题能生成稳定示例 SQL |
| M2-4 | mock SQL 执行返回 | P0 | 待测试 | M2-1 | 返回稳定表格数据 |
| M2-5 | mock 模式端到端测试 | P0 | 待测试 | M2-2/M2-3/M2-4 | 无 API Key、无 DB 时完整跑完 |

需要用户测试：

| 测试项 | 状态 | 操作 | 期望结果 |
|---|---|---|---|
| 数据查询案例 | 未开始 | 输入“本月中东公司销量多少？” | 能看到完整 SOP 执行链和最终回答 |
| 同环比案例 | 未开始 | 输入“本月终端量同比去年怎么样？” | 路由到同环比 Skill |
| 闲聊案例 | 未开始 | 输入“汽车保养一般多少公里做一次？” | 路由到 chat Skill |

### M3：执行器错误处理

目标日期：2026-05-25 至 2026-05-26

| 编号 | 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| M3-1 | 设计标准 StepResult | P1 | 待测试 | M2 | 所有 MCP 返回字段统一 |
| M3-2 | 修改 `execute_mcp` 包装逻辑 | P1 | 待测试 | M3-1 | 异常返回标准错误结构 |
| M3-3 | workflow 识别 `success=false` | P1 | 待测试 | M3-1 | 关键节点失败时立即中断 |
| M3-4 | 前端展示标准错误 | P1 | 待测试 | M3-3 | Streamlit 显示失败节点和错误原因 |
| M3-5 | 增加最小失败用例 | P1 | 未开始 | M3-3 | 401、SQL 失败、缺少参数均可读 |

需要用户测试：

| 测试项 | 状态 | 操作 | 期望结果 |
|---|---|---|---|
| 错误 API Key | 未开始 | 配置错误 API Key 后运行 | 显示 LLM 调用失败，不继续伪造最终答案 |
| 数据库不可达 | 未开始 | real 模式下断开 DB | 显示 DB 连接失败 |
| N2SQL 失败 | 未开始 | mock 一个失败返回 | workflow 中断并显示失败节点 |

### M4：SQL 安全与纠错闭环

目标日期：2026-05-26 至 2026-05-28

| 编号 | 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| M4-1 | 增加 SQL 只读校验 | P1 | 未开始 | M3 | 非 SELECT SQL 被拦截 |
| M4-2 | 增加多语句拦截 | P1 | 未开始 | M4-1 | 包含多个语句时被拒绝 |
| M4-3 | 增加表白名单 | P1 | 未开始 | M4-1 | 非允许表不可查询 |
| M4-4 | 增加查询超时和行数限制 | P1 | 未开始 | M4-1 | 慢查询和大结果集受控 |
| M4-5 | 接入 SQL 纠错节点 | P1 | 未开始 | M3 | SQL 执行失败后可修正并重试 |
| M4-6 | 限制纠错重试次数 | P1 | 未开始 | M4-5 | 最多重试 1-2 次 |

需要用户测试：

| 测试项 | 状态 | 操作 | 期望结果 |
|---|---|---|---|
| 危险 SQL | 未开始 | 构造包含 `DROP` 的 SQL | 被拦截 |
| 多语句 SQL | 未开始 | 构造 `SELECT 1; SELECT 2` | 被拦截 |
| 错误字段 SQL | 未开始 | mock N2SQL 输出错误字段 | 进入 SQL 纠错 |
| 空结果 | 未开始 | 查询无数据条件 | 返回友好解释 |

### M5：配置与知识治理

目标日期：2026-05-28 至 2026-05-29

| 编号 | 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| M5-1 | 拆出表结构配置 | P2 | 未开始 | M1-3 | 表结构不再硬编码在 N2SQL 里 |
| M5-2 | 拆出指标和字段口径配置 | P2 | 未开始 | M5-1 | 字段说明可配置维护 |
| M5-3 | 统一模型 provider 配置 | P2 | 未开始 | M1-3 | 模型名、base URL、temperature 集中管理 |
| M5-4 | 统一 Skill/MCP schema 命名 | P2 | 未开始 | M3 | 文档只保留一套主规范 |
| M5-5 | 更新 README | P2 | 未开始 | M1-M4 | 包含安装、mock、real、扩展说明 |

需要用户测试：

| 测试项 | 状态 | 操作 | 期望结果 |
|---|---|---|---|
| 修改表配置 | 未开始 | 改配置文件中的字段描述 | N2SQL prompt 使用新描述 |
| 切换模型配置 | 未开始 | 修改模型配置 | LLM/N2SQL 使用新配置 |
| 新人启动验证 | 未开始 | 按 README 从零启动 | 10 分钟内能跑 mock |

## 5. 问题与阻塞

| 编号 | 问题 | 影响 | 状态 | 处理方案 | 负责人 |
|---|---|---|---|---|---|
| B1 | 源码存在默认 DB 凭据 | 安全风险高 | 待测试 | 已移到 `.env`，提交 `.env.example` | Codex |
| B2 | 无 API Key 时无法完整离线演示 | 阻塞前端和链路调试 | 待测试 | 已实现 mock LLM/N2SQL/SQL | Codex |
| B3 | `requirements.txt` 缺少实际依赖 | 新环境启动失败 | 待测试 | 已补 `langgraph`、`pytz`、`python-dotenv` | Codex |
| B4 | MCP 失败会被当最终答案 | 用户体验差，调试困难 | 待测试 | 已实现标准返回补齐与失败中断 | Codex |
| B5 | SQL 安全仅靠关键词 | 真实数据库风险高 | 进行中 | 已增加单语句 SELECT、表白名单、行数上限；后续补超时和只读账号验证 | Codex |
| B6 | 表结构和业务口径散落 | 维护成本高 | 未处理 | 拆到配置文件 | 待定 |
| B7 | Dify 远程 host 疑似仍为占位 | 远程知识库未真实命中，当前走本地 fallback | 有问题 | 替换真实 Dify host 或确认 Dify 网络路由后复测 `knowledge_retrieval` | 用户/Codex |
| B8 | Streamlit 默认组件视觉不统一 | 前端体验粗糙，思考链和操作区交互不够清晰 | 进行中 | 已替换会话操作区和思考链展示；后续继续统一聊天区、输入区、错误态和配置区 | Codex |
| B9 | `web_search` MCP 未进入业务 Skill 流程 | 虽然 MCP 健康检查通过，但用户对话中不会主动联网检索，实时知识能力不可见 | 未开始 | 评估是否新增联网问答 Skill，或在 chat/data/yoy Skill 中增加条件路由与 `web_search` 步骤 | Codex |

## 6. 待测试清单

### 6.1 自动化测试待补

| 测试文件 | 测试内容 | 状态 |
|---|---|---|
| 待定 | `resolve_variables` 基础替换 | 未开始 |
| 待定 | `resolve_variables` 字典/列表类型保留 | 未开始 |
| `tests/test_registries.py` | MCP 注册表加载 | 已完成 |
| `tests/test_registries.py` | Skill 注册表加载 | 已完成 |
| `tests/test_workflow.py` | mock 模式 `run_agent` 端到端 | 已完成 |
| `tests/test_mcp_contracts.py` | SQL 安全拦截 | 已完成 |
| `tests/test_workflow.py` | workflow 失败中断 | 已完成 |
| `scripts/check_mcps.py` | MCP mock 可调用性与 real 缺配置优雅失败 | 已完成 |
| `tests/test_mcp_contracts.py` | MCP 返回契约与 real 缺配置优雅失败 | 已完成 |
| `tests/test_health_check_script.py` | MCP 健康检查脚本回归测试 | 已完成 |

### 6.2 用户手动测试

| 编号 | 测试场景 | 输入 | 状态 | 期望 |
|---|---|---|---|---|
| U1 | 普通数据查询 | 2023年6月中东公司的销量（包括批发量和终端量）是多少？ | 技术通过 | 完整执行 data_query；返回批发量 2160、终端量 2311 |
| U2 | 同比环比查询 | 2023年6月中东公司终端量同比去年怎么样？ | 技术通过，待业务确认 | 路由到 yoy_yoy_analysis；去年同期为 0，百分比无意义 |
| U3 | 闲聊问题 | 汽车保养一般多少公里做一次？ | 技术通过 | 路由到 chat |
| U4 | 无 API Key | 不配置 API Key 运行 | 未开始 | mock 模式跑通 |
| U5 | 错误 API Key | 配置错误 Key | 未开始 | 显示清晰错误 |
| U6 | DB 不可达 | real 模式断开数据库 | 未开始 | 显示连接错误 |
| U7 | 危险 SQL | 生成或传入 DROP/DELETE | 未开始 | 被安全拦截 |
| U8 | 思考链运行态交互 | 输入真实或 mock 问题并观察执行中状态 | 待测试 | 执行过程中 timeline 展开可见，步骤随 callback 逐步追加，完成后仍可折叠/展开 |
| U9 | 侧边栏会话操作区 | 切换会话后查看置顶、改名、删除按钮 | 技术通过，待用户确认 | 三个按钮等高、字号稳定、不再出现 emoji 图标错位 |
| U10 | 前端窄屏布局 | 内置浏览器 654px 宽度下操作页面 | 待测试 | 侧边栏、聊天内容、输入框不遮挡、不溢出 |
| U11 | 联网检索链路 | 输入需要实时外部信息的问题 | 未开始 | 思考链中出现 `web_search` 调用，并把联网结果纳入最终回答 |

### 6.3 正式 MCP 测试前置清单

以下条件满足后，再开始 real 模式正式 MCP 测试，并需要用户提供或确认配置：

| 编号 | 前置条件 | 状态 | 说明 |
|---|---|---|---|
| R1 | 可用模型 API Key | 已通过 UAT-02 核验 | 用于测试 `llm`、`n2sql` 真实调用 |
| R2 | 模型 base URL 与模型名 | 已通过 UAT-02 核验 | 默认 DeepSeek，也可切换 OpenAI 兼容服务 |
| R3 | 只读数据库账号 | 已通过 UAT-02 核验 | 必须只读，避免 SQL 执行风险 |
| R4 | 数据库网络连通性 | 已通过 UAT-02 核验 | 本机需能访问 DB host/port |
| R5 | 允许查询表白名单 | 待用户确认 | 已支持 `DB_ALLOWED_TABLES`，real 测试前需确认允许表 |
| R6 | 查询超时与 LIMIT 策略 | 待测试 | 已支持 `SQL_MAX_ROWS`、`DB_CONNECT_TIMEOUT`、`DB_READ_TIMEOUT`、`DB_WRITE_TIMEOUT` |
| R7 | 真实测试问题清单 | 已启动 | 第一轮覆盖数据查询、固定日期同环比、闲聊、MCP 健康与 Streamlit 烟测 |

## 7. 每日记录

### 2026-05-22

已完成：

| 内容 | 结果 |
|---|---|
| 全面阅读项目 | 明确项目是 Skill/MCP 双层 ChatBI PoC |
| 架构合理性评估 | 方向合理，实现仍偏演示化 |
| 问题识别 | 识别安全、mock、依赖、错误处理、SQL 闭环等问题 |
| 创建开发计划文档 | 已生成 `doc/后续开发计划_codex_20260522.md` |
| 调整文档形态 | 从规划文档改为每日排期跟踪文档 |

未完成：

| 内容 | 原因 | 后续动作 |
|---|---|---|
| 移除 DB 硬编码凭据 | 本轮只整理文档 | 下一步优先做 |
| 完整 mock 模式 | 本轮只整理文档 | M2 开始实现 |
| SQL 安全增强 | 需要先完成执行器错误处理 | M4 实现 |

发现的问题：

| 问题 | 影响 | 状态 |
|---|---|---|
| 无 API Key 时 `llm` 会请求 DeepSeek 并 401 | mock 演示不完整 | 未处理 |
| `requirements.txt` 缺少 `langgraph` | 新环境可能运行失败 | 未处理 |
| 源码存在默认数据库凭据 | 安全风险 | 未处理 |

需要用户测试：

| 测试项 | 状态 |
|---|---|
| 确认本文档格式是否满足每日推进 | 待测试 |

### 2026-05-23

今日计划：

| 顺序 | 任务 | 状态 |
|---|---|---|
| 1 | 新增 `.env.example` | 未开始 |
| 2 | 移除 DB 硬编码凭据 | 未开始 |
| 3 | 补齐依赖 | 未开始 |
| 4 | 设计统一配置模块 | 未开始 |

已完成：

| 内容 | 结果 |
|---|---|
| 暂无 | 暂无 |

问题与阻塞：

| 问题 | 状态 |
|---|---|
| 暂无 | 暂无 |

需要用户测试：

| 测试项 | 状态 |
|---|---|
| 暂无 | 暂无 |

### 2026-05-24

今日计划：

| 顺序 | 任务 | 状态 |
|---|---|---|
| 1 | 实现 mock 模式开关 | 未开始 |
| 2 | 实现 mock LLM | 未开始 |
| 3 | 实现 mock N2SQL | 未开始 |
| 4 | 实现 mock SQL 执行 | 未开始 |

已完成：

| 内容 | 结果 |
|---|---|
| 暂无 | 暂无 |

问题与阻塞：

| 问题 | 状态 |
|---|---|
| 暂无 | 暂无 |

需要用户测试：

| 测试项 | 状态 |
|---|---|
| mock 模式数据查询端到端 | 未开始 |
| mock 模式同环比端到端 | 未开始 |
| mock 模式闲聊端到端 | 未开始 |

### 2026-06-01

今日计划：

| 顺序 | 任务 | 状态 |
|---|---|---|
| 1 | 拉取并确认最新代码状态 | 已完成 |
| 2 | 打开内置浏览器并启动 Streamlit mock 页面 | 已完成 |
| 3 | 根据浏览器批注修复侧边栏会话操作区 | 已完成 |
| 4 | 根据浏览器批注重构思考链 UI 完成态 | 已完成 |
| 5 | 根据浏览器批注重构思考链 UI 运行态 | 已完成，待浏览器复验 |
| 6 | 第二轮收敛聊天主区、输入区、侧边栏和推荐问题交互 | 已完成 |
| 7 | 浏览器复验和全量 pytest 回归 | 已完成 |
| 8 | 提交并推送前端修复 | 已完成 |
| 9 | 更新后续开发计划文档 | 进行中 |

已完成：

| 内容 | 结果 |
|---|---|
| 侧边栏操作按钮修复 | 去掉 `📌/✏️/🗑️` emoji，改为稳定纯文本按钮，并统一高度、字号、行高、列宽 |
| 思考链完成态修复 | 历史消息不再使用 `st.expander` 默认 UI，改为 Codex 风格 timeline |
| 思考链运行态修复 | 新问题执行时不再使用 `st.status`，callback 会刷新同一个 timeline 容器 |
| 聊天主区第二轮收敛 | 统一数据结果、图表、业务解读、口径说明标题样式，新增口径说明卡片 |
| 推荐问题交互修复 | 点击首页推荐问题后不再残留首页空态，直接进入聊天态 |
| 输入区与窄屏检查 | 510px 宽度下无横向溢出，底部输入区未遮挡当前内容 |
| 前端 helper 测试 | 新增 `streamlit_ui_helpers.py` 与 `tests/test_streamlit_ui_helpers.py`，覆盖 HTML escaping 与稳定布局 class |
| 自动化回归 | `python3 -m pytest -q`：24 passed，1 warning |
| Streamlit 启动方式调整 | 本地验证使用 `127.0.0.1` 绑定和关闭 usage stats，减少网络探测导致的连接错误 |
| Git 推送 | 已提交并推送 `962c712 Improve Streamlit chat UI traces` 到 `origin/main` |

发现的问题：

| 问题 | 影响 | 状态 |
|---|---|---|
| Streamlit 默认组件样式难以完全统一 | 视觉一致性差，按钮、expander、状态组件容易出现默认图标或尺寸异常 | 进行中 |
| 运行中思考链需要慢场景验证 | mock 闲聊执行太快，难观察逐步刷新效果 | mock 数据查询技术通过，real 慢查询待用户继续观察 |
| 内置浏览器偶发连接错误 | 用户验收时可能看到 Connection error 遮挡页面 | 已通过本地绑定启动缓解，需继续观察 |

下一步动作：

| 顺序 | 任务 | 优先级 | 验收标准 |
|---|---|---|---|
| 1 | 用户人工验收第二轮前端效果 | P1 | 侧边栏、聊天主区、思考链、输入区整体可接受 |
| 2 | 错误态页面设计 | P1 | API Key 错误、DB 错误、Dify fallback 都有清晰且统一的提示 |
| 3 | README/运行说明更新 | P1 | 新人能按说明启动 mock/real 和本地 Streamlit |
| 4 | Dify 远程知识库真实验证 | P1 | 替换真实 host 或确认网络路由后复测 |

需要用户测试：

| 测试项 | 状态 |
|---|---|
| 在内置浏览器确认侧边栏按钮是否满意 | 待确认 |
| 在内置浏览器输入一个数据查询，观察思考链运行中展示 | 技术通过，待用户确认 |
| 指出下一个最丑或最影响使用的位置 | 待确认 |

### 2026-06-02

今日计划：

| 顺序 | 任务 | 状态 |
|---|---|---|
| 1 | 根据浏览器批注继续收敛 Streamlit 侧边栏和首页 | 已完成 |
| 2 | 补齐同一会话内的会话记忆 | 已完成 |
| 3 | 更新后续开发计划文档 | 进行中 |
| 4 | 验证、提交并推送到 Git | 进行中 |

已完成：

| 内容 | 结果 |
|---|---|
| 会话操作入口 | 会话右侧保留 `⋯`，操作改为小浮层，不再占用侧栏宽度 |
| 新建会话入口 | 从突兀大按钮调整为侧栏“会话”标题行右侧 `+ 新建` 小按钮 |
| 首页空态 | 已移除“推荐快速测试案例”和四个快捷问题按钮，仅保留标题、说明和输入框 |
| 打字机效果 | 新回答先逐段输出，完成后再切回表格、图表、业务解读等结构化展示 |
| 会话记忆 | `run_agent` 支持传入同一会话最近消息，当前问题会结合最近上下文理解指代 |
| mock 记忆验收 | mock chat 可回答“刚才我问了什么？”这类基础记忆问题 |
| 自动化回归 | `python3 -m pytest -q`：30 passed，1 warning |

发现的问题：

| 问题 | 影响 | 状态 |
|---|---|---|
| `web_search` MCP 尚未被业务 Skill 主动调用 | 联网检索能力只停留在 MCP 健康检查层面，真实对话中不可见 | 新增待办 B9/W7/U11 |
| 指代类问题路由仍较粗 | mock 规则中含“公司”等关键词的问题可能误路由到数据查询 | 待后续优化路由或新增上下文意图识别规则 |

下一步动作：

| 顺序 | 任务 | 优先级 | 验收标准 |
|---|---|---|---|
| 1 | 联网检索 Skill 编排补齐 | P1 | 实时信息问题能触发 `web_search`，思考链和最终回答可见联网证据 |
| 2 | 指代/上下文路由优化 | P1 | “刚才查的是哪个公司”等问题优先使用会话记忆，不误触发 SQL 查询 |
| 3 | README/运行说明更新 | P1 | 新人能按说明启动 mock/real 和本地 Streamlit |

## 8. 变更日志

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-05-22 | 创建文档 | 第一版为后续开发规划 |
| 2026-05-22 | 改为排期跟踪版 | 增加状态、里程碑、今日任务、问题阻塞、测试清单、每日记录 |
| 2026-05-30 | 启动 UAT 跟踪 | 标记真实 API Key 已替换，新增 UAT 第一轮用例与前置状态 |
| 2026-06-01 | 更新前端重构跟踪 | 记录侧边栏操作区、思考链完成态/运行态修复，以及下一步 UI 重构计划 |
| 2026-06-01 | 更新第二轮前端验收记录 | 记录聊天主区、输入区、侧边栏、推荐问题交互和自动化回归结果 |
| 2026-06-02 | 更新 UI 与会话记忆进展 | 记录侧边栏小浮层、首页空态精简、打字机效果、会话记忆，以及联网检索 Skill 编排待办 |
