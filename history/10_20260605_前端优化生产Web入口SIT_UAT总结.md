# 2026-06-05 前端优化生产 Web 入口 SIT/UAT 总结

## 背景

用户要求按照 `doc/codex_前端优化版本_20260605.md` 开始开发，将当前 ChatBI PoC 从 Streamlit 调试界面升级为独立 Web 生产页面。

本轮完成第一版生产 Web 入口开发，并按用户要求自行执行 SIT/UAT，更新 `history/`、`qa/` 和开发文档。

## 本轮已完成

| 模块 | 结果 |
|---|---|
| SQLite 会话层 | 新增 `chat_repository.py`，支持会话、消息、思考步骤持久化 |
| FastAPI API | 新增 `api_server.py`，提供会话 API、聊天 API、简版 SSE |
| 联网开关路由 | `run_agent` 增加 `web_search_enabled`，关闭时拦截显式联网，开启时内部数据进入内外部综合分析 |
| Web 前端工程 | 新增 `web_frontend/`，React + Vite 工程，复用原型头像和图标资源 |
| 会话 UI | 实现历史侧栏、新建、置顶、重命名、删除 |
| 聊天 UI | 实现消息列表、用户/助手头像、思考过程、步骤详情、结果渲染、输入框、联网开关 |
| 文档 | 更新 README、SIT 报告、UAT 报告和开发文档状态 |

## SIT/UAT 结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 自动化回归 | PASS | `45 passed, 1 warning in 0.24s` |
| MCP mock 健康检查 | PASS | 7 个 MCP 均 OK |
| API 首批业务用例 | PASS | 6 个文档验收问题均按预期路由 |
| 前端静态工程 | PASS | 关键组件、资源、配置文件均存在 |
| 前端构建 | BLOCKED | 当前 PATH 缺少 `npm`/`pnpm`/`yarn`/`tsc` |
| 视觉截图验收 | BLOCKED | 前端 dev server 未能启动，无法做 Playwright 截图对比 |

## 关键验收用例

| 场景 | 联网开关 | 实际路由 | 结果 |
|---|---|---|---|
| 内部数据 | 关闭 | `data_query` | PASS |
| 内部数据 + 外部资料 | 开启 | `data_web_compare_analysis` | PASS |
| 联网公开信息 | 开启 | `web_search_answer` | PASS |
| 联网关闭提示 | 关闭 | 无 Skill，返回开关提示 | PASS |
| 多轮追问竞品对比 | 开启 | `web_compare_analysis` | PASS |
| 同环比分析 | 关闭 | `yoy_yoy_analysis` | PASS |

## OPEN 问题

| 编号 | 问题 | 下一步 |
|---|---|---|
| OPEN-01 | 缺 npm 工具链，无法启动前端和执行 build | 安装 npm 或提供可用包管理器后复测 |
| OPEN-02 | 视觉 95% 复原尚未完成浏览器截图验收 | 前端可启动后用 Playwright 对比原型和新页面 |
| OPEN-03 | 当前 UAT 为 mock 模式 | 真实凭据和网络就绪后补 Real UAT |
| OPEN-04 | SSE 暂为顺序输出 | 下一轮接入 `run_agent` callback 做运行中实时推送 |

## 下一步建议

1. 补齐前端 npm 工具链。
2. 执行 `cd web_frontend && npm install && npm run build`。
3. 启动 `api_server.py` 和 Vite dev server，完成浏览器截图验收。
4. 在 Real 模式下复测内部数据、联网检索、内部 + 外部综合分析。

## 复测更新

用户指出 npm 可由 Codex 自行安装后，已继续完成以下工作：

| 项目 | 结果 |
|---|---|
| 项目内 npm runtime | PASS，使用 `.system_generated/npm-runtime/package/bin/npm-cli.js` |
| 前端依赖安装 | PASS |
| Rollup native 兼容 | 已改用 `@rollup/wasm-node`，规避 Codex.app Node 的 macOS 代码签名限制 |
| 前端 build | PASS，`npm run build` 成功 |
| 前后端 dev server | PASS，FastAPI 8010、Vite 5173 |
| Playwright 交互 smoke | PASS，页面发送内部数据问题并渲染 `data_query` 回答 |
| 截图证据 | `output/playwright/chatbi-web-smoke-20260605.png` |

更新后的剩余事项：

1. 做原型页面与生产 Web 页的并排截图对比，继续修正视觉细节。
2. 补 Real 模式 UAT。
3. 接入真正运行中的 SSE 事件，而不是顺序输出。
